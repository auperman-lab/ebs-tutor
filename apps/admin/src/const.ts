export const api = 'https://api-stage.escolalms.com/';

export const routes = {
  main: '/',
  login: '/login',
  register: '/register',
};

export const apiEndpoints = {
  register: '/api/auth/register',
};
