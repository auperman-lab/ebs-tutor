import { createStyles } from 'antd-style';

export const useStyles = createStyles(({ token, responsive }) => ({
  container: {
    width: '100%',
    marginTop: 40,
  },

  select: {
    width: '100%',
  },
}));
