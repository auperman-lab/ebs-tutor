import { createStyles } from "antd-style";

export const useStyles = createStyles(({ css }) => ({
  wrapper: css`
    background-color: white;
    min-height: 420px;
    height: 100%;
    width: 100%;
  `,


}));
