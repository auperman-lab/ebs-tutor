import { Flex } from 'antd';
import { useStyles } from './NotificationSettingsStyles';

export const NotificationsSettings = () => {
  const { styles } = useStyles();
  return (
    <Flex vertical className={styles.mainBlock}>
      ertyhujki
    </Flex>
  );
};
