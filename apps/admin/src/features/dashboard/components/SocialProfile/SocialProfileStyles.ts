import { createStyles } from 'antd-style';

export const useStyles = createStyles(({ token }) => ({
  mainPart: {
    width: '80%',
    padding: 40,
    backgroundColor: 'white',
  },
  heading: {
    lineHeight: 1,
  },
  saveButton: {
    // marginTop: 94,
  },
}));
